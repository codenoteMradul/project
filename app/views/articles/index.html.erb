<h1>index</h1>

<ul>
	<% @article.each do |article| %>
		<li>
			<%= link_to "show the articles", article %>
			<li><%= link_to "Destroy", article_path(article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
   </li>

	<% end %>
  
  <br>

	<li>
		<%= link_to "new article",new_article_path %>

	</li>
</ul>